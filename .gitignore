.env
.venv/
__pycache__/
*.pyc
*.pyo
data/agent_memory.db
*.db-shm
*.db-wal
.DS_Store

# GRPO model weights — checkpoints ignored, but the latest trained adapter
# is committed so the team can run the fine-tuned policy without retraining
data/grpo_weights/*
!data/grpo_weights/adapter/
data/research_run.log
